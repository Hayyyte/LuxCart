const Loayout = ({ children }: { children: React.ReactNode }) => {
    return (
        <>
            {/* Navbar */}
            {children}
            {/* Footer */}
        </>
    );
}
 
export default Loayout;